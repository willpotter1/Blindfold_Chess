Stockfish engine integration findings (session summary)
-------------------------------------------------------

Engine files and status
- Using /public/stockfish/stockfish-nnue-16-single.js (copied from node_modules/stockfish/src/stockfish-nnue-16-single.js) and /public/stockfish/stockfish-nnue-16-single.wasm. Network shows 200 for WASM, script 304 (cached), size ~575 KB.
- Manual probes in the browser prove the engine works:
  - Worker created with new Worker('/stockfish/stockfish-nnue-16-single.js'), posting UCI/isready/setoption/position/go returns bestmove (e.g., d2d4 or e2e4) and emits uciok/readyok.
  - Engine understands plain string UCI commands; no wrapper objects.

Current app symptoms
- runEngineSelfTest() still times out on "go", steps: init/uci/setoption/position pass, go fails with "Stockfish command timeout".
- runEngineDebug() times out / returns unknown types because the prior debug helper is no longer meaningful; main issue is command handling, not engine files.

Probable cause
- App worker wiring may not be seeing bestmove or may be running stale code. Manual probe works, so engine is fine. The app must:
  - Use worker URL: /stockfish/stockfish-nnue-16-single.js.
  - Resolve readiness on both uciok and readyok before issuing next commands.
  - Post plain string commands (postMessage(command)).

Code state (key files)
- src/lib/chessEngine/stockfishWorker.ts
  - Worker URL: /stockfish/stockfish-nnue-16-single.js
  - readyQueue resolves on readyok and uciok
  - sendCommand posts strings; bestmove responses are matched to a queue
  - waitForReady posts isready with a 5s timeout
- src/lib/chessEngine/getEngineMove.ts
  - Calls sendCommand('uci') → waitForReady → setoption → waitForReady → position → waitForReady → go movetime …

What to check next when resuming
1) Ensure the browser is using the latest bundle (restart dev server, hard-refresh, disable cache). Confirm Network shows /stockfish/stockfish-nnue-16-single.js requested by the app.
2) Add temporary logging in stockfishWorker.ts onmessage to confirm bestmove is being received during the self-test.
3) If bestmove arrives but timeout still occurs, ensure messageQueue handling matches the bestmove data and that sendCommand(go, true) is pushing onto the queue exactly once.

Known good manual sequence (proven):
  const w = new Worker('/stockfish/stockfish-nnue-16-single.js');
  w.onmessage = (e) => console.log(e.data);
  w.postMessage('uci');
  w.postMessage('isready');
  w.postMessage('setoption name Skill Level value 5');
  w.postMessage('position startpos');
  w.postMessage('isready');
  w.postMessage('go movetime 300');
  // returns bestmove within ~300ms
